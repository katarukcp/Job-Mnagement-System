package com.job.constants;

/**
 * Jobs available in system, this enum can have the as many jobs as we need 
 * and those can be read from configuration file or from database.
 * @author Surendra
 *
 */
public enum JobType {
	EMAIL, DWH;
}
